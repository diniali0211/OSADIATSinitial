
  # ATS Resume Analyzer Dashboard (Community)

  This is a code bundle for ATS Resume Analyzer Dashboard (Community). The original project is available at https://www.figma.com/design/Xg61fSzHrfHvz8NCF60sDD/ATS-Resume-Analyzer-Dashboard--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  