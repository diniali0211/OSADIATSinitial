[phases.setup]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-eng", "tesseract-ocr-msa"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port 8000"
